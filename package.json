{
    "scripts": {
        "build": "spago build",
        "dev": "pnpm build && vite dev"
    },
    "devDependencies": {
        "purescript": "^0.15.15",
        "purs-tidy": "^0.11.0",
        "spago": "^0.93.29",
        "vite": "^5.2.12"
    },
    "dependencies": {
        "@tanstack/virtual-core": "^3.5.0"
    }
}
